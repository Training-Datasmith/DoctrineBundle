doctrine:
    dbal:
        default_connection: default
        connections:
            default:
                dbname: db

    orm:
        entity_managers:
            default:
                hydrators:
                    test_hydrator: Symfony\Bundle\Doctrine_Bundle\Tests\Dependency_Injection\Test_Hydrator
                mappings:
                    XmlBundle: ~
